### Accessing Dataset
This repository uses [**Git LFS**](https://git-lfs.com/) to store large files (`creditcard.csv`). 

Make sure Git LFS is installed before running:
```bash
git lfs install
git lfs pull